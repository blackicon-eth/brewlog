import React from "react";
import { ComingSoon, FlaskIcon } from "../components/ui";

// Placeholder for brewing tools (timer, ratio + water calculators). Built out later.
export function ToolsScreen() {
  return (
    <ComingSoon
      section="Tools"
      title="Dial-in helpers"
      blurb="A brew timer, ratio calculator, and water math — the bench tools for a better cup."
      icon={FlaskIcon}
    />
  );
}
